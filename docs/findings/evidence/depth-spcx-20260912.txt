Routable depth into USDC pairs - 2026-09-12T16:19:22.060Z
US equity market is CLOSED at this timestamp; these are off-hours books.

================================================================================
SPCX
  wrapper     depth@100bps depth@300bpsdepth@1000bps
  SPCX          THROTTLED        $518k    THROTTLED
  SPCXx              $32k        $248k        $443k
  SPCXon        THROTTLED    THROTTLED    THROTTLED
  FRAGMENTED         $32k        $766k        $443k   <- sum of separate books  (INCOMPLETE: a probe was throttled)
  (bar for  pSPCX                         /USDC to beat)

================================================================================
Interpretation: summing separate books OVERSTATES what a fragmented market
delivers to one user - a single order hits one book, not all of them. The
realistic comparison for a user is the BEST single wrapper, not the sum.
Parity clears the bar if consolidated depth beats the sum; it is already
useful to a user if it beats the best single wrapper.

underlying   best single @100bps    sum @100bps    concentration
SPCX         $32k (SPCXx)           $32k           100% in the deepest
